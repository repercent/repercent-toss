# Repository workflow

이 저장소에서 브랜치와 커밋을 만들 때 아래 규칙을 따른다.

## 브랜치 규칙

### 장기 브랜치

- `main`: 상용 서버 배포 브랜치
- `dev`: 기본 개발 서버 브랜치
- `v2_dev`: `kracker_client` 개발 서버 브랜치

장기 브랜치에서 직접 기능을 개발하지 않는다. 작업 성격에 맞는 작업 브랜치를 만든 뒤 개발 브랜치로 병합한다.

### 작업 브랜치 유형

- `feat`: 기능 개발, 개선, 추가
- `bugfix`: 버그 수정
- `hotfix`: 상용 환경의 긴급 수정
- `refactor`: 기능 변화 없는 코드 구조 또는 성능 개선
- `chore`: 빌드 설정, 라이브러리 업데이트, 문서화 등 기타 작업

Jira 태스크가 있는 작업은 Jira의 **브랜치 만들기**에서 제공한 이름을 기준으로 한다. 저장소에서 유형 prefix를 사용하는 경우 `<type>/<Jira가 생성한 이름>` 형식으로 만들고, Jira 키와 한글 설명을 임의로 바꾸지 않는다.

예시:

```bash
git switch -c bugfix/KA-2-매입-검색-시-엔터로-검색-가능하도록-개선
```

Jira가 정확한 `git checkout -b ...` 또는 `git switch -c ...` 명령을 제공했다면 그 이름을 우선한다. Jira 태스크가 필요한 작업인데 키가 주어지지 않았다면 임의의 키를 만들지 말고 사용자에게 확인한다.

## 커밋 규칙

커밋 제목은 `<대괄호 태그><Jira가 생성한 변경 내용>` 형식으로 작성한다. 태그 뒤에는 공백을 넣지 않는다.

사용 가능한 태그:

- `[Add]`: 개발 또는 추가
- `[Update]`: 개선 또는 수정
- `[Bug]`: 버그 수정
- `[Hotfix]`: 긴급 수정
- `[Delete]`: 삭제
- `[Test]`: 테스트

Jira 태스크의 브랜치 생성 코드에서 브랜치 이름 부분을 변경 내용으로 사용한다.

예시:

```text
Jira 생성 명령: git checkout -b KA-2-매입-검색-시-엔터로-검색-가능하도록-개선
커밋 제목: [Bug]KA-2-매입-검색-시-엔터로-검색-가능하도록-개선
```

하나의 커밋에는 가능한 한 하나의 논리적 변경만 포함한다. 커밋을 요청받았지만 Jira 변경 내용 또는 적절한 태그가 불명확하면 커밋 전에 사용자에게 확인한다.
